# ── 核心（必须） ────────────────────────────────
requests>=2.31
PyYAML>=6.0                     # 可配置解析规则 parsers/*.yaml
python-dotenv>=1.0              # config.py 加载 .env（缺失会导致环境变量读不到）
numpy>=1.26                     # injector/textify.py 模块级 import（服务启动必需）

# ── 数据源（可选，按需安装） ──────────────────────
yfinance>=0.2                  # VIX/DXY/US10Y

# ── ML（可选，ML_SERVICE_URL 配置时联动 ml-service） ────
# Kronos 推理已拆分到独立 ml-service（projects/ml-service/），本服务仅 HTTP 调用。

# ── REST API（可选，仅 --api 模式需要） ────────────
flask>=3.0
waitress>=3.0                  # 生产级 WSGI server

# ── 开发 ────────────────────────────────────────
pytest>=8.0
