{
  "name": "@0xinfrax/chain-rpc-sdk",
  "version": "0.1.0",
  "description": "InfraX Chain RPC SDK — thin wrapper over @0xinfrax/infrax-dk exposing the chain-rpc gateway surface (read /v1/rpc/:chain, broadcast /v1/broadcast/:chain with independent broadcast key, status/health + MQ-16 subscription plans). Same-source, synced releases: re-exports only, no duplicated implementation.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc",
    "prepublishOnly": "npm run build"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "@0xinfrax/infrax-dk": "^0.7.0"
  },
  "keywords": [
    "infrax",
    "chain-rpc",
    "rpc",
    "gateway",
    "broadcast",
    "subscription"
  ],
  "license": "MIT",
  "author": "InfraX",
  "devDependencies": {
    "typescript": "^5.0.0"
  }
}
