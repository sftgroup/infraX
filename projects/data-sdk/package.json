{
  "name": "@0xinfrax/data-sdk",
  "version": "0.1.0",
  "description": "InfraX Data SDK — thin wrapper over @0xinfrax/infrax-dk exposing the data & ML service surfaces (bars/snapshots/factors/ticker/symbols/ml-predictions + ml-service realtime inference). Same-source, synced releases: re-exports only, no duplicated implementation.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc",
    "prepublishOnly": "npm run build"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "@0xinfrax/infrax-dk": "^0.7.0"
  },
  "keywords": [
    "infrax",
    "data",
    "ml",
    "market-data",
    "kline",
    "factors",
    "prediction"
  ],
  "license": "MIT",
  "author": "InfraX",
  "devDependencies": {
    "typescript": "^5.0.0"
  }
}
