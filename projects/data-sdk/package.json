{
  "name": "@0xinfrax/data-sdk",
  "version": "0.1.1",
  "description": "InfraX Data SDK — thin wrapper over @0xinfrax/infrax-dk exposing the data & ML service surfaces (bars/snapshots/factors/ticker/symbols/ml-predictions + factor-factory ml_factory + ml-service realtime inference). Same-source, synced releases: re-exports only, no duplicated implementation.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc",
    "prepublishOnly": "npm run build"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "@0xinfrax/infrax-dk": "^0.8.4"
  },
  "keywords": [
    "infrax",
    "data",
    "ml",
    "market-data",
    "kline",
    "factors",
    "prediction"
  ],
  "publishConfig": { "access": "public" },
  "license": "MIT",
  "author": "InfraX",
  "devDependencies": {
    "typescript": "^5.0.0"
  }
}
