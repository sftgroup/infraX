{
  "name": "@0xinfrax/dc-sdk",
  "version": "0.1.0",
  "description": "InfraX DC SDK — thin wrapper over @0xinfrax/infrax-dk exposing the DC service surface (on-chain data: events/tokens/chains + MQ-16 data subscriptions). Same-source, synced releases: re-exports only, no duplicated implementation.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc",
    "prepublishOnly": "npm run build"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "@0xinfrax/infrax-dk": "^0.7.0"
  },
  "keywords": [
    "infrax",
    "dc",
    "onchain",
    "data",
    "events",
    "subscription"
  ],
  "publishConfig": { "access": "public" },
  "license": "MIT",
  "author": "InfraX",
  "devDependencies": {
    "typescript": "^5.0.0"
  }
}
