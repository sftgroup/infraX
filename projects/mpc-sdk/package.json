{
  "name": "@0xinfrax/mpc-sdk",
  "version": "0.1.0",
  "description": "InfraX MPC SDK — lightweight independent client for the MPC wallet microservice (email-based key-shard wallets). Wallet module (sendCode/register/recover/status/createWallet) + Session module (unlock/lock/status). No dependency on infrax-dk.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc",
    "prepublishOnly": "npm run build"
  },
  "engines": {
    "node": ">=18"
  },
  "keywords": [
    "infrax",
    "mpc",
    "wallet",
    "session",
    "key-shard",
    "email-recovery"
  ],
  "license": "MIT",
  "author": "InfraX",
  "devDependencies": {
    "typescript": "^5.0.0"
  }
}
