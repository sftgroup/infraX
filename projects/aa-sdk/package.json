{
  "name": "@infrax/aa-sdk",
  "version": "0.1.0",
  "private": true,
  "description": "InfraX 共享 ERC-4337 Smart Account SDK（白标自 PocketX @pocketx/aa-sdk）— Kernel v3 + UserOp v0.7 + Bundler + Paymaster + Session Key",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "peerDependencies": {
    "permissionless": ">=0.2.0",
    "viem": ">=2.0.0"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "permissionless": "^0.3.7",
    "typescript": "^5.9.3",
    "viem": "^2.55.10",
    "vitest": "^3.2.4"
  }
}
