# ml-service dependencies（独立模型推理服务 :9120）
fastapi>=0.100.0
uvicorn[standard]>=0.23.0
python-dotenv>=1.0.0
requests>=2.31.0
numpy>=1.24.0
pandas>=2.0.0
yfinance>=0.2.30

# ── LightGBM 方向预测（TREE_ML_ENABLED=true 时启用）────
# 纯 CPU 训练，2C4G 足够（样本数千行 × 数十特征，秒级训练）
lightgbm>=4.0.0
scikit-learn>=1.3.0
joblib>=1.2.0

# ── XGBoost / Random Forest 方向预测（P1 对比家族）────────
# 与 LightGBM 同数据集/同切分训练作对照；RF 用 sklearn 自带（已含）
xgboost>=2.0.0

# ── FinBERT 文本情绪（FINBERT_ENABLED=true 时启用）────
# 部署机启用需另装 torch：pip install torch --index-url https://download.pytorch.org/whl/cpu
transformers>=4.40.0
huggingface-hub>=0.23.0

# ── Kronos 波动率（KRONOS_ENABLED=true 时启用）────────
# 依赖 torch + transformers + einops + safetensors，且需 clone Kronos 源码：
#   git clone https://github.com/shiyu-coder/Kronos /home/ubuntu/Kronos
#   systemd 单元加 Environment="PYTHONPATH=/home/ubuntu/Kronos"
einops>=0.7.0
safetensors>=0.8.0
huggingface-hub>=1.5.0
