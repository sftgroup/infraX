{
  "name": "@0xinfrax/infrax-dk",
  "version": "0.9.0",
  "description": "InfraX JavaScript SDK — wallet, multisig vault, payment (x402), SaaS tenants, on-chain data (DC), MPC key-shard wallets, OKX ChainOS market data, InfraX data service (bars/snapshots/factors/ticker/symbols/ml-predictions), ml-service realtime inference and MQ-16 plan subscriptions (dc/market/chain-rpc/mpc + payments invites/transfers/batch).",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc",
    "prepublishOnly": "npm run build"
  },
  "engines": {
    "node": ">=18"
  },
  "keywords": [
    "infrax",
    "wallet",
    "web3",
    "multisig",
    "mpc",
    "vault",
    "saas",
    "market-data",
    "x402"
  ],
  "license": "MIT",
  "author": "InfraX",
  "devDependencies": {
    "typescript": "^5.0.0"
  }
}
