{
  "name": "@0xinfrax/infrax-dk",
  "version": "0.5.1",
  "description": "InfraX JavaScript SDK — wallet, multisig vault, payment (x402), SaaS tenants, on-chain data (DC), MPC key-shard wallets, OKX ChainOS market data, InfraX data service (bars/snapshots/factors/ticker/symbols/ml-predictions) and ml-service realtime inference (tree/volatility/bolt/moirai/timesfm/consensus).",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc",
    "prepublishOnly": "npm run build"
  },
  "engines": {
    "node": ">=18"
  },
  "keywords": [
    "infrax",
    "wallet",
    "web3",
    "multisig",
    "mpc",
    "vault",
    "saas",
    "market-data",
    "x402"
  ],
  "license": "MIT",
  "author": "InfraX",
  "devDependencies": {
    "typescript": "^5.0.0"
  }
}
